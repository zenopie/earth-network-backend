services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - example.env
    ports:
      - "8000:8000"
    volumes:
      - ./:/app:delegated
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped